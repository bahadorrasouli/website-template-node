<!DOCTYPE html>
<html lang="en">
<%- include('components/head', {title}) %>
<body>
    <div style="display: flex; flex-wrap: wrap;">
    <% data.forEach(data => { %>
    
        <%- include('components/card', {data}) %> 
      
    <% }); %>
    </div>
</body>
</html>